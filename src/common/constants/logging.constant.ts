export enum LOG_LEVELS {
  FATAL = 'fatal',
  ERROR = 'error',
  WARN = 'warn',
  LOG = 'log',
  DEBUG = 'debug',
  VERBOSE = 'verbose',
}

export enum LOG_CODES {
  FATAL = 0,
  ERROR = 1,
  WARN = 2,
  LOG = 3,
  DEBUG = 4,
  VERBOSE = 5,
}
